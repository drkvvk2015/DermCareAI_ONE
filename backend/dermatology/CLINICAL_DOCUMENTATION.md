# Dermatology documentation completeness

A lightweight documentation gate prevents incomplete encounters from being
finalized. It is deliberately non-diagnostic: it checks presence of clinician
documentation fields only.

The gate is suitable for UI feedback and server-side validation before an
encounter is marked complete.
